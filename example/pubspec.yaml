name: flip_widget_example
description: FlipWidget examples
publish_to: "none"
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:

  flip_widget:
    path: ../
    
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets: 
    - assets/flutter_cover.png
