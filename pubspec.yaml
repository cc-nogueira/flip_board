name: flip_widget
description: A package for flip widgets
version: 0.0.1
homepage: https://github.com/cc-nogueira/flip_widget

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
  assets: 
    - assets/flutter_cover.png
